import Game from '../game';

describe("Game", () => {
    describe("Valid", () => {
        it("new", () => {
            //var game = new Game(null, null, null);
            var teste = [1, 2];
            var b = teste.findIndex(t => t == 1);
            
            console.log("b= " + b);
        });
    });
});